# ── Copyrighted source material ──────────────────────────────────────────
# Book texts, epubs, and derived markdown are licensed works and must never
# be committed to this (public) repository. Keep them local only.
source material/**
!source material/
!source material/README.md

# Common ebook / derived-text formats, wherever they appear
*.epub
*.mobi
*.azw
*.azw3
*.pdf

# The ingest pipeline's source spine: a book's title plus its chapter-title
# table in reading order reconstructs that source's outline (D-10, D-74; see the
# 2026-08-31 addendum under D-74). Local only; the .example ships with
# placeholder rows.
tools/spine.local.json
!tools/spine.local.json.example

# ── Python ───────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ── Environment / secrets ────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── OS / editor cruft ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# ── Local-private planning state ─────────────────────────────────────────
# Discovery ledgers, deliberations, and working notes for the planning
# sessions. Never published: this repository is public.
.local/
