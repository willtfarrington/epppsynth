# CANARY FIXTURE (EP-6, check `modality`) — the retired stem, outside the exemption table

D-4 retired the clinical-modality expansion of this project's name; the slug stays opaque. The
owner-ratified exemption table (ruling OD-10) has three entries, and this file is not one of them,
so the stem below must be a finding.

The sweep is a **stem** sweep, not a token sweep, because EP-2's exact-token grep could not see the
adjectival form. Both forms appear below so the widening is exercised rather than assumed:

    psychotherapy
    psychotherapeutic

Neither line describes this project. Both are here so that a scanner nobody has watched fail is not
mistaken for one that works.
