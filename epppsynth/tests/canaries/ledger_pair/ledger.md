# CANARY FIXTURE for check nine, private half — a synthetic ledger, authored here

Standing in for a file under `.local/`, holding nothing private: every sentence below was invented.
Its own prose deliberately shares no wording with the public half's prose, so that the only findings
are the two passages planted on purpose.

Those two are shared with the public half deliberately, and the check must treat them differently.

The first is the planted finding. It sits in the public half's preamble prose, outside any entry:
this synthetic paragraph was copied straight out of the working notes and never belonged in a public
file at all.

The second is expected and must not be a finding, because in the public half it sits inside a
published decision entry: this settled statement appears in both halves of the pair on purpose,
because a decision recorded in the words it was settled in is this log doing its job.
