# CANARY FIXTURE (EP-6, check `ledger`) — a synthetic public decision log

Both halves of this pair were authored here. Proving the ledger check works never requires reading
or copying the real `.local/`, which is the point: the check exists so that private planning state
stays private, and a fixture that had to borrow from it would defeat its own purpose.

The sentence that follows is the planted finding, and it sits in the **preamble prose** rather than
inside an entry: this synthetic paragraph was copied straight out of the working notes and never
belonged in a public file at all.

## Index

| # | Title | Decides |
|---|---|---|
| D-1 | A synthetic decision | Something invented for this fixture. |

---

**D-1 A synthetic decision.** This settled statement appears in both halves of the pair on purpose,
because a decision recorded in the words it was settled in is this log doing its job. *Why:* the
refined invariant (D-2, owner ruling OD-3) makes shared text inside an entry expected and shared
text in the surrounding prose a finding.
