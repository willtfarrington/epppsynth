CANARY FIXTURE (EP-6, check `secrets`) — deliberate, inert, and not a credential.

Both strings below are structurally valid token shapes with bodies that are a repeated placeholder
character. Nothing here authenticates against anything; no issuer has ever minted a token whose body
is a run of X. The prefix has to be the real one, because the prefix is what the rule matches: a
scanner proven only against a prefix nobody uses is a scanner proven against nothing.

    ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

    password = "XXXXXXXXXXXXXXXX"
