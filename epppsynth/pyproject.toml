[project]
name = "epppsynth"
version = "0.0.0"
description = "Local, offline-capable reflection aid — deterministic core library with a CLI."
license = "Apache-2.0"
requires-python = ">=3.14"
# The concept registry and the source rights record are YAML (D-15), so a YAML
# reader is a runtime dependency of the library and not a dev tool. It is the
# only one: `epppsynth.rights` is otherwise stdlib, which keeps the no-model CI
# path (D-42) cheap and keeps the licence checks runnable without a network.
dependencies = [
    "pyyaml>=6.0.2",
]

[project.scripts]
epppsynth = "epppsynth.cli:main"

[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/epppsynth"]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.pytest.ini_options]
addopts = "--strict-markers"
testpaths = ["tests"]
markers = [
    "requires_index: needs the local derived index; never runs in CI (no-model path, D-42)",
    "requires_model: needs local model weights; never runs in CI (no-model path, D-42)",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
